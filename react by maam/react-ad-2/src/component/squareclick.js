   // const count = [10, 15, 20, 25, 30, 35, 40];
    // let nobtn;
    // if (this.state.no) {
    //   nobtn = count.map((number) =>
    //     number % 2 == 0 && number * number
    //   );
    // } else {
    //   nobtn = count.map((number) =>
    //   number % 2 == 1 && number * number)
    // }